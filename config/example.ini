[general]
pidfile_path: /var/run/changefinder.pid
window_sec: 600

[changefinder]
r: 0.01
k: 1
T1: 10
T2: 10

[datadog]
queries:
  system.cpu.idle{*}by{host}
  avg:system.disk.free{*}.rollup(avg, 60)
